[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
monitor-core = { path = "../core", features = ["flash"] }
serde_json = "1.0"
