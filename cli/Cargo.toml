[package]
name = "quota-dock-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
quota-dock-core = { path = "../core", features = ["flash"] }
serde_json = "1.0"
